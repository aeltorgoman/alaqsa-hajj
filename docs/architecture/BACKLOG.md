# سجلّ المؤجَّل — Backlog & Technical Debt

**ما رُصد ولم يُبنَ.** كل بند هنا **لا يمنع الإطلاق** — وإلا لكان في الخارطة لا في هذا السجلّ.

> **قاعدة العمل:** ما يمنع التنفيذ أو يكسر الأمن أو يكسر المعمارية **يوقف العمل ويُبلَّغ**.
> وما هو تحسينٌ أو تطويرٌ مستقبليّ **يُسجَّل هنا ويُكمَل التنفيذ** بلا نقاش معماري جديد.

**آخر تحديث:** بعد إغلاق س٤ وس٥ ونشرهما.

---

## ١. ديون معمارية — لها قرار معتمد ووقتٌ محدَّد

| # | البند | القرار | متى |
|---|---|---|---|
| د١ | **`user-admin` تقرأ الصلاحية مباشرة** من `user_profiles` بدل `has_permission()` | السلوك مطابق · لا يُعاد فتح س٢ | مهمّة مواءمة مستقلّة |
| د٢ | **الجلب الشامل للحجاج عند الإقلاع** — يمنع تضييق سياسة القراءة في RLS | **Long-Term Architecture** · S4 §٣.٢.١ | بعد الانتقال إلى الجلب الخاصّ بكل ميزة |
| د٣ | **نموذج الأدوار** — ٥ تركيبات صلاحيات لـ٦ مستخدمين، أي لا أدوار فعلياً | مؤجَّل صراحةً · §٥.٣ | قرار مستقلّ |
| د٤ | ~~**إعادة توليد `database.ts`** بعد نشر ترحيل س٤~~ — أُغلق مع نشر س٤ | S4 §١٠.٠ بند ت٤ | ✅ |
| د٥ | ~~**`verify_user` · `create_user` · `update_user` معطّلة**~~ — حُذفت في س٥ مع `public.users` | §٩.١ | ✅ |

---

## ٢. شيفرة ميتة ونظافة

| # | البند | الأثر |
|---|---|---|
| ~~ن١~~ | ~~**`src/utils/helpers.ts` ملفّ يتيم**~~ — حُذف في س٦ / الخطوة ١ (ق٥) · ESLint ٢٩١ ← ٢٨٧ | ✅ |
| ن١٢ | **أعمدة `*_url` تحفظ مفاتيح كائنات لا روابط** بعد س٦ — الاسم صار مضلّلاً. إعادة التسمية تلمس ١٦ ملفاً و`database.ts` والبوابة بلا مكسب أمنيّ (ق٤) | تنظيف مستقبليّ خالص |
| ن٢ | **عشرة مفاتيح صلاحيات قديمة في القاعدة** غير معرّفة في `ALL_PERMISSIONS` | خاملة — `has_permission()` لا تُستدعى بها |
| ن٣ | **`LoginPage` نصّ الحقل الإرشادي** ما زال `admin@company.local` بينما الافتراضي صار `admin@system.local` | تجميليّ |
| ن٤ | **دَين ملفّ القفل** (`package-lock.json`) | مرصود من مراجعات سابقة |
| ن٦ | **`handleDocUpload` في `PassengersPage` بلا التقاط** — `Promise.all([uploadDoc, scanDocument])` بلا `try/catch`، ففشل المسح يترك `docUploading` عالقاً بلا رسالة. رُصد أثناء س٩ ولم يُمسّ (خارج بند الحدّ) | يعطّل رفع مستند واحد حتى تحديث الصفحة |
| ن٧ | **مواضع المسح الثمانية تبتلع `DocumentScanError.message`** وتعرض نصّاً عامّاً — فرسالة تجاوز الحدّ لا تصل المستخدم | تجميليّ · الرسالة في السجلّ |
| ن٥ | **ESLint: ٢٩١ ملاحظة** مرجعاً ثابتاً — لا يزيد ولا يُعالَج دفعةً واحدة | يُخفَّض تدريجياً مع كل ملفّ يُمسّ |

---

## ٣. تحسينات تجربة ورسائل

| # | البند |
|---|---|
| ت١ | **رفض ٤٠٣ من `Scan-passport`** يظهر «تعذر الاتصال بخدمة مسح المستندات» — و`scanDocument` تميّز ٤٠١ وحده. الأثر العملي صفر (من يصل إلى الزرّ يملك الصلاحية) |
| ت٢ | **تحذير ماليّ في معالج إقفال الموسم** — قبل الإقفال تُعرض التحذيرات التشغيلية ولا يُعرض ملخّص ماليّ |
| ت٣ | **`Modal` يُخفي ✕ أثناء التنفيذ** في بعض الحوارات لا كلّها — توحيد السلوك |
| ت٤ | **تنظيف الملفات اليتيمة في التخزين** — مستندات بلا حاج مرتبط |

---

## ٤. أمن مؤجَّل بقرار — مرصود لا مهمَل

| # | البند | متى يُغلق |
|---|---|---|
| أ٨ | **`user-admin` خارج الطبقة المشتركة** — لا `authorize()` ولا حدّ استدعاءات ولا `cors()`. سلوكها الأمني مطابق (تقرأ `is_active` و`manage_users` من `user_profiles` بمفتاح الخدمة) لكنها الدالة الوحيدة التي تكرّر المنطق | مهمّة مواءمة د١ |
| أ١ | **حاوية `passengers-docs` عامة القراءة** — أي رابط مستند يُفتح لمن يعرفه | **س٦** (C3) — غير قابلة للتقسيم |
| أ٢ | **`company_profile_public` يرفع `security_definer_view`** عند Advisors | **مقصود** — مستثنى صراحةً في جرد الوصول المجهول |
| أ٣ | ~~**حدّ معدّل وتصفية استجابة** على دوال Edge~~ — أُغلق في #75 و#76 و#77 | ✅ |
| أ٧ | **`delete_empty_financial_group` ينفّذها `anon` نظرياً** — Advisor يرفعها WARN، وPostgres يرفض استدعاءها فعلياً (`0A000`: دالة محفّز). سحب `execute` من `public` سطرٌ واحد يُغلق الملاحظة | مع أول ترحيل يمسّ الصلاحيات |
| أ٦ | **`user-admin` بلا حدّ استدعاءات** — لا تستعمل الطبقة المشتركة (د١)، فإدخالها يفتح س٢ | مع مهمّة مواءمة د١ |
| أ٤ | ~~**تسريب رسائل القاعدة إلى المستخدم** (M1)~~ — أُغلق في #73 | ✅ |
| أ٥ | **تدوير الأسرار** | **س٩** — مهمّة نشر (ن٩) لا شيفرة |

---

## ٥. قرارات مطروحة غير محسومة

| # | القرار | الحال |
|---|---|---|
| ق١ | **حساب دعم المورّد** (Vendor Support Account) | **غير معتمد** · شروطه الأربعة في Security Architecture §١٨.٨ |
| ق٢ | **نموذج الأدوار** (§٥.٣) | مؤجَّل — تأجيله مجانيّ لأن كل تفويض يمرّ بـ`has_permission()` |
| ق٣ | **بوابة المورّد** (Vendor Portal) | مشروع مستقلّ · ب٠ يخدمه بلا أن يعرفه (أ١١) |
| ق٤ | **جلسة بوابة قصيرة العمر** — الحالة النهائية للبوابة: تحقّق **مرّة واحدة** بوثيقة وميلاد ثم رمز جلسة قصير، بدل إعادة استعمال بيانات الهوية الثابتة **كاعتماد في كل طلب**. `pilgrim-doc` في س٦ حلٌّ انتقاليّ معتمد بقيوده (ق٢) ولا يُعاد تصميم البوابة الآن | مرحلة مستقلّة بعد س٩ |

---

## ٦. مهامّ نشر معلّقة — ليست ديناً بل انتظاراً

| # | المهمة | المنفِّذ |
|---|---|---|
| ~~ن٣~~ | ~~تشغيل أداة الإقلاع~~ · ~~الفحص الحيّ لس٣ (ن٧)~~ · ~~حساب كسر الزجاج~~ — **الثلاثة نُفِّذت** ✅ | — |
| ن٨ | **ضبط سرّ `ALLOWED_ORIGINS`** لدوال Edge — بدونه تبقى ترويسة CORS `*` كما اليوم | صاحب المشروع |
| ن٩ | **تدوير الأسرار** (أ٥) — مفاتيح `ANTHROPIC` و`VAPID` وService Role | صاحب المشروع |
| ن٤ | إغلاق التسجيل الذاتي من لوحة Supabase | صاحب المشروع |
| ~~ن١٠~~ | ~~حذف حساب الفحص `test403@system.local`~~ — **نُفِّذ** ✅ · `auth.users` = ٢ | — |
| ن١١ | **تفعيل حماية كلمات المرور المسرَّبة** (HaveIBeenPwned) من لوحة Auth — Advisor `auth_leaked_password_protection` | صاحب المشروع |

**سببها واحد:** شبكة بيئة التنفيذ محجوبة عن `*.supabase.co` بسياسة تنظيمية، فكل ما يحتاج طلباً حيّاً يُنفَّذ من جهاز صاحب المشروع.

---

## كيف يُستعمل هذا السجلّ

- **عند اكتشاف ملاحظة غير مانعة:** تُضاف هنا فوراً، ويُكمَل التنفيذ.
- **عند بدء مرحلة جديدة:** يُراجَع السجلّ لعلّ بنداً منه صار في نطاقها مجاناً.
- **وما ينتقل إلى الخارطة يُحذف من هنا** — لا يُذكر في مكانين.
